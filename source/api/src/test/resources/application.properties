# Default for tests: Flyway off (enabled explicitly on *IT classes)
spring.flyway.enabled=false

# Avoid DB health checks when JDBC is not configured (e.g. mapper tests)
management.health.db.enabled=false
